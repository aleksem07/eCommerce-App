Copy code
import {{pascalCase name}} from "../{{dashCase name}}/{{dashCase name}}";

describe("{{pascalCase name}}", () => {
  it("should instantiate", () => {
    const instance = new {{pascalCase name}}();
    expect(instance).toBeInstanceOf({{pascalCase name}});
  });
});