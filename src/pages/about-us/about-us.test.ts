import AboutUsPage from "./about-us";

describe("AboutUsPage", () => {
  it("should instantiate", () => {
    const instance = new AboutUsPage();
    expect(instance).toBeInstanceOf(AboutUsPage);
  });
});
