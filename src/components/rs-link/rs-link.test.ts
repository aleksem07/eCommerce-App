import RsLinkComponent from "./rs-link";

describe("RsLinkComponent", () => {
  it("should instantiate", () => {
    const instance = new RsLinkComponent();
    expect(instance).toBeInstanceOf(RsLinkComponent);
  });
});
