import OrderTotalComponent from "./order-total";

describe("OrderTotalComponent", () => {
  it("should instantiate", () => {
    const instance = new OrderTotalComponent();
    expect(instance).toBeInstanceOf(OrderTotalComponent);
  });
});
