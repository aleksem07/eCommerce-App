import FormCheckComponent from "./form-check";

describe("FormCheckComponent", () => {
  it("should instantiate", () => {
    const instance = new FormCheckComponent();
    expect(instance).toBeInstanceOf(FormCheckComponent);
  });
});
