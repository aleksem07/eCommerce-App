import CategoryNavigationListComponent from "./category-navigation-list";

describe("CategoryListComponent", () => {
  it("should instantiate", () => {
    const instance = new CategoryNavigationListComponent();
    expect(instance).toBeInstanceOf(CategoryNavigationListComponent);
  });
});
