import FooterComponent from "./footer";

describe("FooterComponent", () => {
  it("should instantiate", () => {
    const instance = new FooterComponent();
    expect(instance).toBeInstanceOf(FooterComponent);
  });
});
