import PromoCodeComponent from "./promo-code";

describe("PromoCodeComponent", () => {
  it("should instantiate", () => {
    const instance = new PromoCodeComponent();
    expect(instance).toBeInstanceOf(PromoCodeComponent);
  });
});
