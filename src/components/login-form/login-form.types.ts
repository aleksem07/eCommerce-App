export type LoginPageElements = {
  email: HTMLElement;
  password: HTMLElement;
  showPassword: HTMLElement;
  registrationLink: HTMLElement;
};

export interface FormInput {
  key: string;
  value: string;
}
